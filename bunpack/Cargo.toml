[package]
name = "bunpack"
version = "0.1.1"
edition = "2024"
description = "A Rust library for (un)packing binary data into structured formats like python's struct module."
license = "MIT"
repository = "https://github.com/Fabus1184/bunpack"


[dependencies]
bunpack_proc_macros = "0.1.0"
